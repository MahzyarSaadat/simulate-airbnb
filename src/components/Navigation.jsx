import logo from "../assets/react.svg"

const Nav = () => {
    return(
        <nav className="nav--container">
            <img src={logo} alt="logo" />
        </nav>  
    )
}

export default Nav;